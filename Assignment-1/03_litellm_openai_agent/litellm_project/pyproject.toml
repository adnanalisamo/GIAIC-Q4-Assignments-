[project]
name = "litellm-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
litellm-project = "litellm_project:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
